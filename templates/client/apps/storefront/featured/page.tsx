import { ProductGrid } from "@aether/storefront-default";

export default function FeaturedPage() {
  return <ProductGrid initialFlag="featured" heading="Featured products" eyebrow="Featured" description="Products promoted through catalog overrides and rules." />;
}
