import { ProductGrid } from "@aether/storefront-default";

export default function SearchPage() {
  return <ProductGrid heading="Search" description="Search, filter, and sort the full catalog." />;
}
