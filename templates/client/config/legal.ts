/**
 * Bump this whenever your privacy/cookie policy changes - AssistantWidget
 * and ContactForm both send it with every request so consent stays tied to
 * whatever version of the policy the visitor actually agreed to.
 *
 * This template doesn't ship privacy/cookies/terms/returns/shipping pages -
 * that content is genuinely yours to write (see the Aether reference repo's
 * apps/storefront/components/legal-content.ts for the shape one real
 * implementation takes, and apps/storefront/app/{privacy,cookies,...}/
 * for how its pages render it). Add your own routes, then start bumping
 * this version string whenever their content changes.
 */
export const legalPolicyVersion = "1.0.0";
