// Re-exports the default skin's Hero from @aether/storefront-default - this
// file is the override point a generated client edits to ship its own Hero
// instead (see templates/client/apps/storefront's same pattern).
export { Hero } from "@aether/storefront-default";
