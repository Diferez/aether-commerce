# Versioning

Use `pnpm changeset` for consumer-visible package changes: patch for compatible fixes, minor for compatible features and major for breaks. `pnpm version:packages` applies approved versions. Publishing is manual-only through the separate workflow.
